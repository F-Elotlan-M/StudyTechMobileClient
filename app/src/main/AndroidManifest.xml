<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/login"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyTechMobile"
        tools:targetApi="31">
        <activity android:name=".ui.RegistrarAcademico" />
        <activity android:name=".ui.RegistrarEstudiante" />
        <activity android:name=".ui.BarraActivity"/>
        <activity android:name=".ui.BarrAdminActivity"/>
        <activity
            android:name=".ui.Comentarios"
            android:label="Comentarios">
        </activity>

        <activity
            android:name=".ui.Reportar"
            android:label="Reportar">
        </activity>

        <activity
            android:name=".ui.ComentarVideo"
            android:label="Comentar">
        </activity>

        <activity
            android:name=".ui.VideoReproduccion"
            android:label="Video">
        </activity>

        <activity
            android:name=".ui.Login"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>